[init]
	defaultBranch = main

[alias]
    cl = clone
    co = checkout
    b = branch
    mr = merge

    a = add
    df = diff
    ts = status
    sh = stash
    rs = reset

    cm = commit
    ps = push
    pl = pull

    lg = log --oneline --graph --decorate --all

[core]
	excludesfile = ~/.gitignore_global
