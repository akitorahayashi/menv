name: Setup Shell Links

on:
  workflow_call:

jobs:
  link-shell:
    runs-on: macos-15
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Homebrew and just
        run: make setup

      - name: Create symbolic links for common shell configuration
        env:
          HOME: ${{ runner.temp }}/home
        run: |
          mkdir -p "$HOME"
          just cmn-shell
