#!/bin/sh
#
# Auto-generated by the menv Ansible role. Do not edit manually.
# This wrapper guarantees commands run from the repository root.
# The repository is now fixed at ~/.menv so it can own all of its artifacts.

set -euo pipefail

MENV_ROOT="${HOME}/.menv"

cd "$MENV_ROOT"

if [ "$#" -eq 0 ]; then
  exec "${SHELL:-/bin/sh}"
fi

exec "$@"
