#!/bin/sh
#
# Auto-generated by the menv Ansible role. Do not edit manually.
# This wrapper guarantees commands run from the repository root.

set -euo pipefail

MENV_ROOT="{{ repo_root_path }}"

if [ -z "$MENV_ROOT" ]; then
  echo "menv: repository path not configured" >&2
  exit 1
fi

cd "$MENV_ROOT"

if [ "$#" -eq 0 ]; then
  exec "${SHELL:-/bin/sh}"
fi

exec "$@"
