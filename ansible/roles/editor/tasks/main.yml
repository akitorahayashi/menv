---
- name: "Setup VS Code"
  include_tasks: vscode.yml
  when: editor_install_vscode | default(true) | bool
  tags: [editor, editor-vscode]

- name: "Setup Cursor"
  include_tasks: cursor.yml
  when: editor_install_cursor | default(false) | bool
  tags: [editor, editor-cursor]